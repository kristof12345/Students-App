@page "/portfolios/{id}"
@inherits AppPage

@if (LoginService.User == null)
{
    <InvestmentApp.Webpage.Components.Login.LoginForm />
}
else
{
    <InvestmentApp.Webpage.Pages.PortfolioDetailsPage PortfolioId=@long.Parse(id) />
}

@code
{
    [Parameter]
    public string id { get; set; }
}
